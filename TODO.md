[] make `zenops init` without a url bootstrap a new user
[] Decide policy for dirty zenops config repo on `apply`.
[] Module-level rustdoc on public `src/` modules.
[] Rustdoc on the `Output` trait.
[] Start using a `CHANGELOG.md`.
[] Remove the git config command line from README.md by adding support for git config, and then automatically add to it
